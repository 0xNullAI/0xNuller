{
  "name": "@0xnullai/llm-proxy-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "test": "node ../../scripts/run-vitest.mjs run --root ../.. --config vitest.config.ts --project worker-proxies"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  }
}
